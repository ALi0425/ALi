# 📋 项目配置模板

在此文件中配置你的项目信息，页面会自动读取并渲染。

---

## 一、球面能力标签 (`sphere.js` → `TAG_DATA`)

每个标签是一段白色文字，大小可调，散落在球体表面。

```js
{ text: '你的标签文字', fontSize: 22 },
```

| 字段 | 说明 | 示例 |
|------|------|------|
| `text` | 标签文字 | `'n8n低代码平台'` |
| `fontSize` | 字号(px)，建议 18~28 | `22` |

**当前标签列表：**
- 国产ERP 信息化项目 (22px)
- 全程项目管理 (20px)
- n8n低代码平台 (20px)
- vibe coding全栈交付 (18px)
- 4年企业级B端系统 (24px)
- 华北理工大学 (18px)
- 工作流自动化 (20px)
- RAG与知识图谱 (22px)
- 业务逻辑抽象 (18px)
- 大型项目协同 (24px)
- 结构化文档编制 (18px)
- 工作流编排 (20px)
- 28岁 (22px)
- 多模态增量清洗 (18px)
- 复杂业务流程解构 (20px)
- 权限协同与资源调配 (22px)
- 大模型落地产品 (24px)

---

## 二、项目卡片 (`sphere.js` → `PROJECT_DATA`)

每个项目会出现在球体表面，双击后变为可拖拽的浮动卡片。

```js
{ label: '项目名称', key: '唯一标识', desc: '简短描述' },
```

| 字段 | 说明 | 示例 |
|------|------|------|
| `label` | 项目名称（显示在卡片上） | `'OmniSight 策略导航'` |
| `key` | 唯一标识（英文，用于关联详情） | `'a1'` |
| `desc` | 一行简短描述 | `'n8n · GraphRAG · JSON Mode'` |

**当前项目列表：**

| key | 名称 | 描述 |
|-----|------|------|
| `a1` | OmniSight 策略导航 | n8n · GraphRAG · JSON Mode |
| `a2` | RARE 智能逆向 | 元数据逆向 · 嵌套画布 · Schema |
| `b1` | 科技管理 一/二期 | 20+流程 · 12805项成果 · 60%+ |
| `b2` | 科技管理 应用三期 | 万级数据 · 14家二级单位 |
| `doc` | 大厂正规军 交付物 | 技术规范 · 可研 · 需规 |

> ⚠️ `key` 必须保持唯一，且与下面终端面板中的 key 对应。

---

## 三、终端面板详情 (`terminal.js` → `NODE_PROFILES`)

点击浮动卡片时弹出的 macOS 风格终端面板，显示项目详情。

```js
a1: {
  title: '显示标题',
  path: '~/projects/omnisight/README.md',
  content: `自由文本内容
支持多行

用空行分段
- 可以用列表
- 也可以用符号`,
},
```

| 字段 | 说明 |
|------|------|
| `title` | 面板标题 |
| `path` | 模拟的文件路径 |
| `content` | 正文内容（自由文本，支持空行和符号） |

**当前已有 profile key：** `a1`, `a2`, `b1`, `b2`, `doc`

新增项目时，在 `NODE_PROFILES` 中添加与 `PROJECT_DATA.key` 同名的条目。

---

## 四、连线 Schema (`connections.js` → `SCHEMA`)

控制哪些项目之间可以连线。

```js
'fromKey→toKey': true,  // 允许连线
```

默认允许：
| 起点 | 终点 | 说明 |
|------|------|------|
| `b1` | `a1`, `a2` | 传统项目 → AI 项目 |
| `b2` | `a1`, `a2` | 传统项目 → AI 项目 |
| `doc` | `a1`, `a2`, `b1`, `b2` | 交付物 → 全部 |

---

## 五、Capability 气泡 (`explosion.js` → `CAPABILITIES`)

Hover 卡片时弹出的能力气泡。

```js
a1: ['能力1', '能力2', '能力3'],
```

每个项目最多 3 个气泡，显示在卡片周围。

---

## 修改流程

1. 编辑此文档中的对应章节
2. 将修改同步到对应的 `src/*.js` 文件
3. 运行 `npm run dev` 预览
4. 或直接编辑 `src/*.js` 文件后刷新浏览器即可

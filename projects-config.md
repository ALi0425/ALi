# 📋 项目配置模板

在此文件中配置你的项目信息，页面会自动读取并渲染。

---

## 一、球面能力标签 (`sphere.js` → `TAG_DATA`)

每个标签是一段文字（绿色或白色），散落在球体表面。

```js
{ text: '你的标签文字', fontSize: 22, color: '#ffffff' },
```

| 字段 | 说明 | 示例 |
|------|------|------|
| `text` | 标签文字 | `'大语言模型应用评估'` |
| `fontSize` | 字号(px)，建议 20~30 | `28` |
| `color` | 颜色 | `'#22c55e'`（绿色）或 `'#ffffff'`（白色） |
| `glow` | 可选，发光效果 | `'0 0 20px rgba(34,197,94,0.4)'` |

**当前标签列表：**
- 大语言模型应用评估 (28px, 绿色)
- Agent 工作流设计 (26px, 白色)
- RAG/GraphRAG 混合检索架构 (26px, 绿色+发光)
- Function Calling (24px, 白色)
- Prompt Engineering (24px, 绿色)
- 端到端交付体系搭建 (28px, 白色)
- 中后台架构 (26px, 白色)
- Axure RP (22px, 白色)
- Claude (30px, 绿色+发光)
- Hermes (24px, 白色)
- 近5年企业级B端系统 (28px, 白色)
- 华北理工大学 (20px, 白色)

---

## 二、项目卡片 (`sphere.js` → `PROJECT_DATA`)

每个项目会出现在球体表面，双击后变为可拖拽的浮动卡片。

```js
{ label: '项目名称', key: '唯一标识', desc: '简短描述' },
```

| 字段 | 说明 | 示例 |
|------|------|------|
| `label` | 项目名称（显示在卡片上） | `'OmniSight 知识图谱与蓝海分析'` |
| `key` | 唯一标识（英文，用于关联详情） | `'a1'` |
| `desc` | 一行简短描述 | `'n8n · GraphRAG · LLM'` |

**当前项目列表：**

| key | 名称 | 描述 |
|-----|------|------|
| `a1` | OmniSight 知识图谱与蓝海分析 | n8n · GraphRAG · LLM |
| `a2` | RARE 逆向资产与智能评估 | 元数据逆向 · 嵌套画布 · LLM |
| `b1` | 科技管理应用 | 3期项目 · 7项一级功能 |
| `b2` | 巡查问题在线管理 | 4项一级功能 · 业务逻辑抽象 |
| `b3` | 项目管理应用 | 复杂业务解构 · 53家单位 |

> ⚠️ `key` 必须保持唯一，且与下面终端面板中的 key 对应。

---

## 三、终端面板详情 (`terminal.js` → `NODE_PROFILES`)

点击浮动卡片时弹出的 macOS 风格终端面板，显示项目详情。

```js
a1: {
  title: '显示标题',
  path: '~/projects/omnisight',
  tag: 'AI · 数据工程',
  metrics: ['n8n工作流编排', 'GraphRAG', 'LLM数据提取'],
  video: _BASE + 'o.webm',
  content: `● 业务背景与定位
→ 描述文本
...`,
},
```

| 字段 | 说明 |
|------|------|
| `title` | 面板标题 |
| `path` | 模拟的文件路径 |
| `tag` | 右上角标签 |
| `metrics` | 能力指标列表（视频加载失败时展示） |
| `video` | 演示视频路径 |
| `content` | 正文内容（自由文本，支持空行、● 标题、→ 描述） |

**当前已有 profile key：** `a1`, `a2`, `b1`, `b2`, `b3`

新增项目时，在 `NODE_PROFILES` 中添加与 `PROJECT_DATA.key` 同名的条目。

---

## 四、连线 Schema (`connections.js` → `SCHEMA`)

控制哪些项目之间可以连线。

```js
'fromKey→toKey': true,  // 允许连线
```

当前允许的连线关系：

| 起点 | 终点 | 说明 |
|------|------|------|
| `b1` | `a1`, `a2` | 科技管理 → AI 项目 |
| `b2` | `a1`, `a2` | 巡查问题 → AI 项目 |
| `b3` | `a1`, `a2`, `b1`, `b2` | 项目管理 → 全部 |

连线至 `a1`（OmniSight）触发烟花特效 + "敬请期待！"
连线至 `a2`（RARE）触发 RARE 嵌套画布模拟器

---

## 五、Capability 气泡 (`explosion.js` → `CAPABILITIES`)

Hover 卡片时弹出的能力气泡。

```js
a1: ['能力1', '能力2', '能力3'],
```

**当前配置：**

| 项目 | 气泡 |
|------|------|
| a1 | GraphRAG · n8n工作流编排 · LLM数据提取 |
| a2 | 元数据逆向解析 · 多层嵌套交互画布 · LLM智能评估 |
| b1 | 跨部门资源协同调度 · 复杂B端业务流程解构 · 大规模资产纳管 |
| b2 | 合规化线上双审流程 · 人才自动分配机制 |
| b3 | 存量功能逻辑重构 · 多单位权限划分 · 多模块业务集成 |

---

## 修改流程

1. 编辑此文档中的对应章节
2. 将修改同步到对应的 `src/*.js` 文件
3. 运行 `npm run dev` 预览
4. 或直接编辑 `src/*.js` 文件后刷新浏览器即可

> ⚠️ **注意：** 此文檔为辅助参考，实际数据以 `src/*.js` 源码为准。

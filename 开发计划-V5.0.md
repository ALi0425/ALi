# 🗺️ 李晶晶 AI 资产控制台 V5.0 — 开发计划

> **版本：** 5.0 | **状态：** 🎉 全部四阶段完成 | **更新：** 2026-06-07

---

## 一、项目总览

### 1.1 项目定位

高端 macOS 终端沙盒控制台风格的个人品牌交互网站。将个人资历与项目经历物化为**吸附在 3D 自转球体上的温润白瓷感晶体卡片**，通过四阶段交互状态机构成完整的品牌叙事。

### 1.2 核心技术栈

| 层 | 技术 |
|---|------|
| 3D 引擎 | Three.js r170 + CSS2DRenderer |
| 样式 | Tailwind CSS v4 + Space Mono (Google Fonts) |
| 构建 | Vite 6 |
| 手势识别 | MediaPipe Hands (Stage 2 接入) |
| 物理 | 自实现阻尼低通滤波器 + 陀螺仪 API |

### 1.3 设计令牌

- **背景色：** `#050505`（矩阵极黑）
- **网格：** 4rem `#1a1a1a` 极细线 + `radial-gradient` 遮罩
- **CRT 纹理：** 20% 透明度双线性扫描线
- **卡片材质：** `bg-neutral-950/40 backdrop-blur-sm border border-neutral-800/60`
- **悬停发光：** `radial-gradient(650px at var(--x) var(--y), rgba(34, 197, 94, 0.15), transparent 80%)`
- **强调色：** `rgba(34, 197, 94, 0.6)` — 绿色微光
- **字体：** `Space Mono`, `Courier New`, `monospace`

---

## 二、四阶段核心交互状态机

### 🔵 State 01：初始入场 · 呼吸旋转的 3D 终端粒子球

**状态：** ✅ **已完成**（V5.0 第一阶段交付）

#### 目标视觉
- `#050505` 极黑背景 + 4rem 物理网格 + CRT 扫描线
- 1200 个绿色微光粒子构成的 3D 自转球体
- 6 个白瓷感圆形节点吸附在球体表面
- 左上角绿色呼吸灯状态栏

#### 已实现功能
- [x] 两层绝对定位背景层（网格 + CRT 扫描线）
- [x] Three.js 场景 + CSS2DRenderer 双渲染管线
- [x] 1200 粒子斐波那契球面分布 + 自绘 canvas 光晕纹理
- [x] 自动 Y 轴旋转 + X 轴摆幅呼吸 + 缩放脉冲呼吸
- [x] 粒子透明度 0.45↔0.75 交变呼吸
- [x] 鼠标位移阻尼跟随（lerp 0.055）
- [x] 手机陀螺仪 deviceOrientation 支持
- [x] ★ 纯文字直接贴于球面：李晶晶(36px) + AI 产品经理(18px) + 北京·27岁·4年B端实战(12px) + 国家电网·14家二级单位协同(11px)
- [x] ★ 5 个项目标签（OmniSight / RARE / 科技管理一,二,三期 / 大厂交付物）带二级描述文字
- [x] 左上角系统状态栏（呼吸灯 + `AUTHORIZED_BY_JJ`）
- [x] 底部系统信息流渐入（CURIOSITY / TEAMWORK）
- [x] 引导提示文本渐入
- [x] MediaPipe 检测入口占位（摄像头枚举 + `<video>` 预创建）
- [x] 双击爆炸 Fallback（粒子向外爆散 → 缓动吸附网格交叉点）

#### 剩余打磨
- [ ] Hover 径向走光效果（`radial-gradient(650px at var(--x) var(--y), ...)`）
- [ ] 节点 Hover 弹出能力气泡

---

### 🟡 State 02：手势触发 · 资产颗粒流的大爆炸

**状态：** ✅ **已完成**（V5.0 第二阶段交付 — 2026-06-07）

#### 触发条件
- MediaPipe Hands 检测到手掌从握拳到完全张开
- 鼠标双击（Fallback，已在 State 01 中实现）

#### 动效表现
1. **流体炸开：** 3D 球体粒子呈 cubic-bezier(0.1, 1, 0.1, 1) 向四周爆炸发散
2. **网格吸附：** 粒子最终减速吸附到 4rem 网格线交汇处，化作绿色坐标原点
3. **优雅悬浮：** 6 个核心节点摆脱引力，以流体排斥算法错落悬浮，做 ±5px 慢速浮动

#### 已实现功能
- [x] 单例状态机：IDLE → EXPLODE → SETTLE → FLOATING（含守卫规则 + onEnter 回调）
- [x] 双击检测（280ms 防抖）触发爆炸
- [x] `__triggerExplosion` 全局回调（供 MediaPipe 调用）
- [x] 摄像机投影 `Vector3.project(camera)` 精确网格映射
- [x] 碰撞扩散螺旋算法（同一网格点多粒子占用时扩散）
- [x] 两阶段动画：burst(0-50%) + settle(50-100%)
- [x] 粒子落定后 opacity→0.2, size→0.05（微小绿色坐标原点）
- [x] 节点淡出 → 重新淡入（交错 120ms 延迟）
- [x] ★ 浮动卡片改为圆角矩形（180px宽，14px圆角）+ `::before/after` 连接端点
- [x] 流体排斥算法（O(n²)/5 节点 + 阻尼 + 中心引力 + ±5px 浮动）
- [x] `updateFloatingNodes()` 逐帧更新
- [x] Escape 键复位：粒子 1.5s 缓动回归球面 + 节点重新吸附
- [x] MediaPipe Tasks-Vision CDN 动态加载（@mediapipe/tasks-vision@0.10.18）
- [x] HandLandmarker GPU 模式加载（Google CDN 模型）
- [x] 摄像头生命周期：探测 → 点击激活 → getUserMedia → 640×480 流
- [x] 手势检测：拇指(4)与小指(20)归一化跨度 > 0.55 → OPEN
- [x] 检测节流 ~8fps（120ms 间隔）
- [x] 用户点击引导提示 / 相机指示器激活摄像头
- [x] 无摄像头环境优雅降级（CAMERA: OFFLINE → 双击触发的 Fallback）
- [x] 状态栏文字/颜色按状态更新（IDLE/EXPLODE/SETTLE/FLOATING）
- [x] 全周期复现能力（Explosion → Escape → 再次触发）
- [x] 控制台日志全套（状态转换、Schema Error、手势识别、复位完成）

---

### 🟠 State 03：吹泡泡 Hover 与终端源码面板坠落

**状态：** ✅ **已完成**（V5.0 第三阶段交付 — 2026-06-07）

#### Hover 微交互
- 鼠标滑过项目节点 → 表面泛虹彩
- 周围依次「功能性淡入」弹出 3 个半透明终端风能力气泡

#### Click 交互
- 点击项目节点 → macOS 终端风格面板从屏幕上方平滑拉下（Top-down）

#### 已实现功能
- [x] 浮动卡片 hover → `.iridescent` 虹彩动画（绿→蓝→紫→绿 呼吸）
- [x] 3 个能力气泡按序交错淡入（间隔 100ms），`cubic-bezier(0.1,1,0.1,1)` 缓动
- [x] macOS 红绿灯圆点 UI（红/黄/绿）
- [x] 面板 Top-down `cubic-bezier(0.1, 0.9, 0.1, 1)` 动画，毛玻璃背景
- [x] 路径栏显示 `~/projects/<name>/<file>.tsx`
- [x] 5 个项目各有独立代码内容 + TypeScript 语法高亮
- [x] 行号显示 + keyword/type/string/comment 着色
- [x] 点击背景遮罩 / Escape 关闭面板
- [x] 复位时自动清理气泡和关闭面板

---

### 🔴 State 04：流光连线与 Safari Canvas Diff 沙盒

**状态：** ✅ **已完成**（V5.0 第四阶段交付 — 2026-06-07）

#### 连线操作
- 从卡片端点拖拽 → 绿色曲线流光线条 → 吸附扣合至目标端点

#### Schema 校验
- 有效连线：通电亮起，光粒瀑布快流
- 无效连线：橘色 `#FF5500` 电流过载崩散，控制台输出 `[Schema Error]: Matrix Mismatch`

#### Safari 视窗降落
- 原生风格苹果浏览器视窗从屏幕中央垂直慢速坠落
- 地址栏显示：`rare.lijingjing.ai/canvas_diff_sandbox`
- Canvas Diff 果味染色：`#EF4444/20`（冲击红）/ `#22C55E/20`（扩增绿）
- 0 Token 安全防线（前端 Mock 数据包 + 紫光按钮解锁 + 限流熔断）

#### 已实现功能
- [x] 拖拽连线系统（端点 mousedown → 曲线跟随 → 目标吸附）
- [x] Three.js QuadraticBezierCurve3 弧形流光线条
- [x] 连通性规则引擎（Schema 矩阵：b1/b2→a1/a2 + doc→any）
- [x] 无效连线 → 橘色 `#FF5500` 40 粒子爆散 + 消亡动画
- [x] 有效连线 → 60 粒子沿路径流动动画（time-based UV）
- [x] 多连线 ≥3 唯一节点 → 自动触发 Safari 视窗降落
- [x] Safari 标题栏红黄绿灯 + 半透明地址栏
- [x] Canvas Diff 左侧选择栏（冲突/重构/新增）
- [x] Canvas Diff 右侧拓扑树（红/绿节点染色 + diff-tag 标签）
- [x] 0 Token 安全面板：紫光「解锁算力」按钮 + Mock 数据包标签
- [x] 30 字输入限流 + 24h 2 次熔断计数
- [x] Escape 复位 → 清除所有连线 + 关闭 Safari

**状态：** 📝 **已规划**

#### 连线操作
- 从传统项目节点 → 拉出矩阵绿色流光线条 → 吸附扣合至 AI 节点

#### Schema 校验
- 有效连线：通电亮起，光粒瀑布快流
- 无效连线：橘色 `#FF5500` 电流过载崩散，控制台输出 `[Schema Error]: Matrix Mismatch`

#### Safari 视窗降落
- 原生风格苹果浏览器视窗从屏幕中央垂直慢速坠落
- 地址栏显示：`rare.lijingjing.ai/canvas_diff_sandbox`
- Canvas Diff 果味染色：`#EF4444/20`（冲击红）/ `#22C55E/20`（扩增绿）
- 0 Token 安全防线（前端 Mock 数据包 + 紫光按钮解锁 + 限流熔断）

#### 开发要点
- [ ] 拖拽连线系统（起点 → 终点吸附）
- [ ] three.js CatmullRomCurve3 + 流光着色器
- [ ] 连通性规则引擎（Schema 矩阵）
- [ ] Safari 视窗 Mockup（红黄绿灯 + 半透明地址栏）
- [ ] Canvas Diff 染色动画
- [ ] 紫光按钮 + 算力限流面板

---

## 三、项目架构

### 3.1 目录结构（当前）

```
ali/
├── index.html                  # 骨架 HTML + 两层背景 + UI 层
├── package.json                # 依赖管理
├── vite.config.js              # Vite + Tailwind 配置
├── 开发计划-V5.0.md             # 本文件
└── src/
    ├── main.js                 # 入口：装配模块 + 动画循环 + 状态分派
    ├── style.css               # Tailwind + 自定义主题 + 浮动节点样式
    ├── scene.js                # Three.js 场景 / 相机 / 渲染器
    ├── state.js                # ★ NEW: 单例状态机
    ├── sphere.js               # 粒子球体 + CSS2D 节点
    ├── interaction.js          # 鼠标 / 触摸 / 陀螺仪追踪
    ├── status-bar.js           # 系统状态栏 UI（状态感知）
    ├── explosion.js            # ★ REWRITTEN: 爆炸 + 网格吸附 + 浮动 + 复位
    ├── mediapipe.js            # ★ REWRITTEN: MediaPipe 手势 + 摄像头
    ├── terminal.js             # ★ NEW: 终端面板 + 语法高亮 + 能力数据
    ├── connections.js          # ★ NEW: 拖拽连线 + Schema 校验 + 流光粒子
    └── safari.js               # ★ NEW: Safari 视窗 + Canvas Diff + 0 Token
```

### 3.2 模块依赖图

```
main.js
  ├─ style.css
  ├─ scene.js          ← three.js + CSS2DRenderer
  ├─ state.js          ← singleton (no deps)
  ├─ sphere.js         ← interaction.js (dampMouse), state.js
  ├─ interaction.js
  ├─ status-bar.js     ← state.js (onEnter)
  ├─ explosion.js      ← state.js, three.js (Vector3.project)
  └─ mediapipe.js      ← window.__triggerExplosion (callback)
```

### 3.3 状态流转

```
  [State 01: 自转粒子球]
         │
         ├─ 双击鼠标 ──────────┐
         │                     │
         ▼                     ▼
  [State 02: 大爆炸 + 网格吸附]  ← MediaPipe 手掌张开
         │
         ▼
  [State 02: 节点自由悬浮]
         │
         ├─ [Esc] 复位 ───────→ State 01
         │
         ├─ Hover 节点 ────────┐
         │                     ▼
         │              [State 03: 气泡弹出]
         │                     │
         ├─ Click 节点 ───────┐
         │                    ▼
         │             [终端面板坠落]
         │
         ├─ 拖拽连线 ─────────┐
         │                    ▼
         │             [State 04: 流光连线]
         │                    │
         │              [Safari Canvas Diff]
         │
         └─ 紫光按钮 ───────── [限流算力解锁]
```

---

## 四、开发进度时间线

| 阶段 | 内容 | 优先级 | 状态 |
|------|------|--------|------|
| **Phase 1** | State 01 粒子球 + 环境底座 | P0 | ✅ **已完成** |
| **Phase 2** | State 02 MediaPipe + 大爆炸 | P0 | ✅ **已完成** |
| **Phase 3** | State 03 Hover 气泡 + 终端面板 | P1 | ✅ **已完成** |
| **Phase 4** | State 04 流光连线 + Canvas Diff | P1 | ✅ **已完成** |
| **Post** | 性能优化、兼容性、部署 | P2 | ⏳ 待定 |

---

## 五、启动与开发命令

```bash
# 开发模式
cd "/Users/alidym/Documents/self project/ali"
npm run dev

# 生产构建
npm run build

# 预览构建结果
npm run preview
```

开发服务器默认运行在 `http://localhost:5173`。

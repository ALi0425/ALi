{
  "name": "ali-portfolio-v5",
  "private": true,
  "version": "5.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.1",
    "@tailwindcss/vite": "^4.1.0",
    "tailwindcss": "^4.1.0",
    "vite": "^6.3.0",
    "wrangler": "^4.99.0"
  }
}
